#ifndef BETA_H //if not defined "BETA_H"
#define BETA_H //then define it as "BETA_H"

class beta
{
    public:
        beta();
        void printCrap();
    protected:
    private:
};

#endif // BETA_H
